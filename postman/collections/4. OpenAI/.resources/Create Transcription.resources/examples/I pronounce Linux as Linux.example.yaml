$kind: http-example
request:
  url: "{{baseUrl}}/audio/transcriptions"
  method: POST
  headers:
    Accept: application/json
  body:
    type: formdata
    content:
      - type: file
        key: file
        src: /Users/johannesnicolai/Documents/torvalds-says-linux.mp3
        description: |
          (Required) The audio file to transcribe, in one of these formats: mp3, mp4, mpeg, mpga, m4a, wav, or webm.
      - type: text
        key: model
        value: whisper-1
        description: |
          (Required) ID of the model to use. Only `whisper-1` is currently available.
      - type: text
        key: prompt
        value: Some style context for the transcription.
        description: |
          An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text/prompting) should match the audio language.
        disabled: true
      - type: text
        key: response_format
        value: json
        description: |
          The format of the transcript output, in one of these options: json, text, srt, verbose_json, or vtt.
      - type: text
        key: temperature
        value: "0"
        description: |
          The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.
      - type: text
        key: language
        value: en
        description: |
          The language of the input audio. Supplying the input language in [ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format will improve accuracy and latency.
      - type: text
        key: transcription
        value: srt
        description: |-
          Choose the format for the transcription
          Allowed values:plain text, srt, vtt
          Default value: plain text
        disabled: true
response:
  statusCode: 200
  statusText: OK
  headers:
    Date: Thu, 30 Mar 2023 14:54:56 GMT
    Content-Type: application/json
    Transfer-Encoding: chunked
    Connection: keep-alive
    openai-organization: user-lryarsvrbjl30zf7hotgyuce
    openai-processing-ms: "642"
    openai-version: 2020-10-01
    strict-transport-security: max-age=15724800; includeSubDomains
    x-ratelimit-limit-requests: "3000"
    x-ratelimit-remaining-requests: "2999"
    x-ratelimit-reset-requests: 20ms
    x-request-id: c9727aa86bee8c7f3671ca413d4f79ed
    CF-Cache-Status: DYNAMIC
    Server: cloudflare
    CF-RAY: 7b0130b25e58cb15-DUS
    Content-Encoding: gzip
    alt-svc: h3=":443"; ma=86400, h3-29=":443"; ma=86400
  body:
    type: json
    content: |-
      {
          "text": "Hello, this is Linus Torvalds and I pronounce Linux as Linux."
      }
order: 3000
